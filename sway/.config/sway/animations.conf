# ── Animations ──────────────────────────────────────
# Needs swayfx-git (0.6+, built against wlroots 0.20) — the tagged
# 0.5.3 release has no animations. Windows pop/expand on open & close.
# The only knob exposed is the duration; 0 disables, max 5000.
animation_duration_ms 200
