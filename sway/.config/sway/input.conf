# ── Input ───────────────────────────────────────────

# Touchpad: Windows-style scrolling (down motion scrolls up),
# tap-to-click left as hardware default
input type:touchpad {
    natural_scroll enabled
    dwt disabled
}

# Mice: classic wheel (wheel down = page down), like Windows
input type:pointer {
    natural_scroll disabled
}
